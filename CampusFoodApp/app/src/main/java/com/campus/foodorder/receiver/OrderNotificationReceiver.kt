package com.campus.foodorder.receiver

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

// Lab: Notifications & AlarmManager (Phase 3)
// Purpose: Receives order status broadcast and shows a notification
class OrderNotificationReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        // TODO: Build notification via NotificationHelper
    }
}