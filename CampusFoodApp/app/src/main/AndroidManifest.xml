<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.CampusFood">

        <activity
            android:name="com.campus.foodorder.ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.campus.foodorder.ui.student.StudentDashboardActivity"/>
        <activity android:name="com.campus.foodorder.ui.vendor.VendorDashboardActivity"/>

        <receiver android:name="com.campus.foodorder.receiver.OrderNotificationReceiver"/>

    </application>
</manifest>